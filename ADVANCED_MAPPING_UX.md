# Advanced Mapping UX/UI

สถานะ: **Ready for phased implementation**

เอกสารนี้เป็นแหล่งอ้างอิงหลักของเป้าหมาย UX, คำศัพท์, กติกาธุรกิจ และสถานะหน้าจอของ Advanced Mapping ส่วนลำดับแก้โค้ดและ Prompt สำหรับ AI อยู่ใน [`ADVANCED_MAPPING_IMPLEMENTATION.md`](./ADVANCED_MAPPING_IMPLEMENTATION.md)

หากโค้ดปัจจุบันหรือ Mockup ขัดกับเอกสารนี้ ให้ยึดเอกสารนี้ก่อน แล้วบันทึกช่องว่างของ Backend/API ไว้ใน Implementation Plan ห้ามทำ UI ที่ดูเหมือนใช้งานได้โดยอาศัยตัวเลขหรือข้อมูลจำลอง

## ผู้ใช้และบริบท

- ผู้ใช้เป็นเจ้าหน้าที่ที่เข้าใจข้อมูลสินทรัพย์ แต่ไม่จำเป็นต้องเชี่ยวชาญโครงสร้าง Excel
- ไฟล์ต้นฉบับอาจมีหลายชีต, หัวตารางหลายชั้น, แถวหัวกลุ่ม และชื่อคอลัมน์ที่ไม่ตรงกัน
- ชีตที่แปลงแล้วต้องคงชื่อและลำดับ 53 ช่องตาม Template
- Advanced Mapping เปลี่ยนตำแหน่งข้อมูลระดับคอลัมน์ ไม่แก้ค่าภายในเซลล์และไม่ตัดแถว
- ระบบต้องอธิบายข้อมูลต้นฉบับทุกคอลัมน์และทุกแถวที่ไม่ได้เข้าสู่ผลลัพธ์

## คำศัพท์บนหน้าจอ

ใช้ภาษาไทยเป็นหลัก:

| คำภายในระบบ | คำที่แสดงต่อผู้ใช้ |
| --- | --- |
| Source Column | คอลัมน์จากไฟล์ต้นฉบับ |
| Template/Destination Column | ช่องในไฟล์ผลลัพธ์ |
| Auto Mapping | ระบบแนะนำ |
| Manual Mapping | คุณเลือกเอง |
| Unresolved | ยังไม่ได้ระบุปลายทาง |
| Excluded source column | ไม่นำเข้าพร้อมเหตุผล |
| Stale validation | ต้องตรวจสอบใหม่ |

`53/53` หมายถึงโครงสร้างผลลัพธ์มีชื่อและลำดับช่องครบ ไม่ได้หมายความว่าทุกช่องต้องมีข้อมูลหรือถูก Mapping

## กติกาธุรกิจที่ยืนยันแล้ว

### 1. การเลือกชีตและไฟล์ผลลัพธ์

- ระบบอ่านและแสดงทุกชีตในไฟล์ต้นฉบับ
- เมื่ออัปโหลดเสร็จ ยังไม่เลือกชีตใดให้โดยอัตโนมัติ
- ผู้ใช้เลือกแปลงชีตเดียวหรือหลายชีตได้ และมีคำสั่ง “เลือกชีตข้อมูลสินทรัพย์ทั้งหมด”
- ผลลัพธ์เป็น `.xlsx` หนึ่งไฟล์ โดยชีตข้อมูลสินทรัพย์ที่เลือกแต่ละชีตถูกแปลงเป็นชีตผลลัพธ์แยกกัน
- ชีตสรุป เช่น `แบบ กข.` แสดงในรายการตามปกติ ผู้ใช้เลือกแนบไว้ในผลลัพธ์ได้ แต่ระบบแนบตามต้นฉบับและไม่แปลงเป็น 53 ช่อง
- ชีตสรุปเป็นเอกสารแนบของงานแปลง ไม่รองรับ flow ดาวน์โหลดเฉพาะชีตสรุปโดยไม่มีชีตข้อมูลสินทรัพย์ในระยะนี้
- หากชีตข้อมูลที่เลือกแม้แต่หนึ่งชีตมี Error ให้บล็อกการดาวน์โหลดทั้งชุด ผู้ใช้ต้องแก้ปัญหาหรือยกเลิกเลือกชีตนั้นก่อน
- Warning ไม่บล็อกการดาวน์โหลด

### 2. โครงสร้างและช่องบังคับ

- ทุกชีตที่ถูกแปลงต้องมี 53 ช่องตาม Template เดียวกัน
- ทุกแถวสินทรัพย์ที่ส่งออกต้องมี `รหัสสินทรัพย์`, `ชื่อสินทรัพย์` และ `ประเภทสินทรัพย์`
- ช่องอื่นที่ไม่มีข้อมูลให้คงว่าง ไม่เป็น Error เพียงเพราะไม่ได้ Mapping
- แสดง “โครงสร้างถูกต้อง” แยกจาก “พร้อมนำเข้าเว็บไซต์” เสมอ

### 3. Source-first Mapping

- มุมมองหลักเริ่มจากคอลัมน์ต้นฉบับ แล้วให้เลือกช่องในไฟล์ผลลัพธ์
- คอลัมน์ต้นฉบับที่มีข้อมูลต้องมีผลลัพธ์หนึ่งสถานะ:
  1. ระบบแนะนำปลายทาง
  2. ผู้ใช้เลือกปลายทางเอง
  3. ไม่นำเข้าพร้อมเหตุผล
  4. ยังไม่ได้ระบุปลายทาง
- คอลัมน์ที่ “ยังไม่ได้ระบุปลายทาง” บันทึกเป็น Draft ได้ แต่ยังดาวน์โหลดไม่ได้
- หนึ่งคอลัมน์ต้นฉบับใช้ได้กับหนึ่งช่องผลลัพธ์ และหนึ่งช่องผลลัพธ์ถูกใช้ได้ครั้งเดียว หากกติกา Parser เดิมจำเป็นต้องใช้ค่าซ้ำ ต้องแสดงเป็นกติกาที่ตรวจสอบย้อนกลับได้ ไม่ทำซ้ำแบบซ่อนเร้น
- ช่องผลลัพธ์ที่ไม่มีข้อมูลแสดงว่า “เว้นว่างตามต้นฉบับ” ไม่แสดงเป็น Error อัตโนมัติ
- มีมุมมองรอง “ดูตาม 53 ช่องผลลัพธ์” สำหรับตรวจโครงสร้างปลายทาง

เหตุผลมาตรฐานของ “ไม่นำเข้า”:

| Reason code | ข้อความบนหน้าจอ |
| --- | --- |
| `unsupported_destination` | เว็บไซต์ปลายทางไม่มีช่องรองรับ |
| `duplicate_information` | เป็นข้อมูลซ้ำกับคอลัมน์อื่น |
| `helper_or_grouping` | เป็นคอลัมน์ช่วยคำนวณหรือจัดกลุ่ม |
| `not_asset_data` | ไม่เกี่ยวข้องกับข้อมูลสินทรัพย์ |
| `other` | อื่น ๆ |

เมื่อเลือก `other` ต้องกรอกรายละเอียดเพิ่มเติม ห้ามมีคำสั่ง “ละเว้นทั้งหมด”

### 4. หัวกลุ่มและลำดับชั้นข้อมูล

- แถวหัวกลุ่มที่ไม่มีรหัสและชื่อสินทรัพย์ไม่ถูกส่งออกเป็นแถวสินทรัพย์
- ค่าหัวกลุ่มถูกส่งต่อให้แถวสินทรัพย์ด้านล่างจนกว่าจะพบหัวกลุ่มใหม่
- ลำดับที่ต้องรองรับคือ `ประเภทสินทรัพย์` → `ชนิดสินทรัพย์` → `ชื่อสินทรัพย์` เช่น ประเภทกว้างเป็น `ครุภัณฑ์` และชนิดย่อยเป็น `โต๊ะ`
- คำจริงที่ยอมรับในแต่ละระดับต้องตรวจจาก Reference ของ Template ก่อนเขียนกฎ Parser ห้าม hard-code จากตัวอย่างในเอกสารนี้
- หากต้นฉบับระบุระดับไม่ชัด ระบบเสนอ Mapping ได้ แต่ต้องให้ผู้ใช้ยืนยันว่าเป็นประเภทหรือชนิด
- หากแถวมีรหัสและชื่อสินทรัพย์ แต่ยังหาประเภทที่ยืนยันแล้วไม่ได้ ให้เป็น Error

### 5. การรักษาและแปลงค่า

- Mapping ที่ผู้ใช้เลือกเองคัดลอกค่าต้นฉบับตรงตัว ช่องว่างคงว่าง
- Parser แปลงได้เฉพาะรูปแบบทางเทคนิคที่มีกติกาชัดเจน เช่น การประกอบวันที่หลายช่อง
- เมื่อ Parser เปลี่ยนรูปแบบค่า Preview ต้องแสดงค่าต้นฉบับเทียบผลลัพธ์
- ระบบไม่สร้างค่าเริ่มต้น ไม่คาดเดาค่าที่ไม่มี และไม่แก้ความหมายของข้อมูลแทนผู้ใช้

### 6. Duplicate และสถานะตรวจสอบ

- รหัสสินทรัพย์ซ้ำเป็น Warning และเก็บทุกแถว
- แถวที่ซ้ำกันทั้งแถวต้องแสดงตำแหน่งที่ซ้ำ แต่ยังไม่บังคับ checkbox ยืนยัน
- เมื่อ Mapping หรือโครงสร้าง Header เปลี่ยน ผลตรวจเดิมเป็นข้อมูลเก่าและสถานะเปลี่ยนเป็น “ต้องตรวจสอบใหม่”
- สีเขียวใช้เมื่อเงื่อนไขที่แสดงครบจริง ไม่ใช้แทนความหมายว่า “ไม่มีโอกาสสูญเสียข้อมูล”

### 7. Draft

- Draft อยู่ตลอด session
- การสลับชีตหรือยกเลิกเลือกชีตชั่วคราวไม่ลบ Mapping และเหตุผลที่บันทึกไว้
- Revalidation ที่ล้มเหลวไม่ลบ Draft
- Draft ถูกล้างเมื่อผู้ใช้สั่ง “ล้าง Draft” หรืออัปโหลดไฟล์ใหม่
- การเก็บ Draft หลัง Refresh/ปิด Browser และ Audit Trail แบบถาวรอยู่ใน Phase ภายหลัง

### 8. ขอบเขตการแก้ข้อมูล

- Advanced Mapping ไม่มีการแก้ค่าเซลล์
- Advanced Mapping ไม่มีการตัดแถวหรือเลือกไม่ส่งออกเป็นรายแถว
- ปัญหาระดับแถวแสดงแบบอ่านอย่างเดียวในระยะนี้
- Row Reconciliation ต้องรายงานแถวที่ระบบไม่ส่งออกพร้อมเหตุผลจากระบบ ไม่เปิดทางให้ผู้ใช้ลบแถวจากหน้านี้

## โครงสร้างหน้า

### A. Sheet Selection

- แสดงทุกชีตพร้อมชนิด: ข้อมูลสินทรัพย์ / ชีตสรุป / ไม่รองรับ
- ชีตข้อมูลมี action “เลือกแปลง”
- ชีตสรุปมี action “แนบตามต้นฉบับ”
- แสดงจำนวนชีตที่เลือกและสถานะรวมของชุดที่เลือก

### B. ภาพรวมการจัดวางข้อมูล

แสดงข้อมูลจริงของชีตที่กำลังดู:

1. “โครงสร้างไฟล์ผลลัพธ์” — `53/53`
2. “ข้อมูลต้นฉบับที่ระบุปลายทางแล้ว” — used/non-empty source count
3. “จำนวนแถวข้อมูล” — พบจากต้นฉบับ / พร้อมส่งออก / ต้องตรวจสอบ
4. “ต้องตรวจสอบ” — จำนวนรายการที่กดไปยังปัญหาได้

ไม่แสดง Warning ดิบที่ผู้ใช้ไม่มีสิ่งให้ทำ

### C. Advanced Mapping

- Mapping ครบแล้ว: เริ่มแบบย่อ
- มีคอลัมน์ยังไม่ระบุปลายทาง: เปิดอัตโนมัติ
- จำสถานะเปิด/ปิดแยกรายชีตตลอด session
- มี Search และ Filter: ทั้งหมด / ยังไม่ได้ระบุปลายทาง / ระบบแนะนำ / คุณเลือกเอง / ไม่นำเข้า
- แต่ละรายการแสดงชื่อคอลัมน์ต้นฉบับ, ตัวอย่างค่าจริง 3–5 ค่า, ปลายทาง, สถานะ และเหตุผล
- ปลายทางที่ถูกใช้อยู่เลือกซ้ำไม่ได้ และต้องบอกว่าใช้อยู่กับคอลัมน์ใด
- Mapping ที่ผู้ใช้เปลี่ยนมี action “คืนค่าระบบแนะนำ”

### D. Preview และ Row Reconciliation

- เปรียบเทียบข้อมูลต้นฉบับกับผลลัพธ์อย่างน้อย 5 แถว
- ไฮไลต์เฉพาะช่องผลลัพธ์ที่ได้รับผลจากการเปลี่ยน Mapping
- แสดง Before/After เมื่อ Parser แปลงรูปแบบค่า
- แสดงแถวที่ไม่ถูกส่งออก พร้อมชื่อชีต เลขแถวต้นฉบับ และเหตุผล
- เมื่อผลตรวจเก่า CTA เปลี่ยนเป็น “ตรวจสอบอีกครั้ง”

## Responsive และ Accessibility

- Desktop ใช้ Review shell ความกว้างเดิมและความหนาแน่นแบบงานข้อมูล
- หน้าจอแคบเรียง Mapping เป็นแนวตั้ง: ต้นฉบับ → ตัวอย่าง → ปลายทาง → สถานะ/action
- ทุก input มี label และข้อความผิดพลาดที่เชื่อมด้วย `aria-describedby`
- ใช้งาน Search, Filter, Select, Reset และ CTA ด้วย keyboard ได้
- Focus state มองเห็นชัด และรองรับ reduced motion

## Visual language

- พื้นหลังฟ้าเทาอ่อน, Card สีขาว, Border ม่วงอ่อน
- Indigo เป็นสี action หลัก
- Teal ใช้กับสถานะที่ผ่านเงื่อนไขจริง
- Amber ใช้กับ Warning และ “ต้องตรวจสอบใหม่”
- มุม 12px และความหนาแน่นกะทัดรัด
- ใช้ Inter ร่วมกับ Noto Sans Thai
- HTML/Stitch เดิมเป็นเพียงแนวทางด้านสี ระยะห่าง และ Card; โครงสร้าง Source-first ในเอกสารนี้มีอำนาจเหนือกว่า

## ลำดับส่งมอบ

1. Sheet Selection และภาพรวมจากข้อมูลจริง
2. Source-first Mapping
3. “ไม่นำเข้า” พร้อมเหตุผลและ Source Coverage
4. Preview, stale validation และ Draft ต่อชีต
5. Backend rules, Row Reconciliation และการแนบชีตสรุป
6. Browser/Responsive QA
7. Header Structure: Header 1–3 แถว, Data Start/Data End และ Mapping Diff
8. Persistent Draft และ Audit Trail

งานรอบแรกจบที่ข้อ 6 ห้ามเริ่มข้อ 7–8 ก่อนข้อ 1–6 ผ่านเกณฑ์ใน Implementation Plan

## Acceptance criteria ระดับผลิตภัณฑ์

- ผู้ใช้เลือกแปลงหนึ่งหรือหลายชีต และเลือกแนบชีตสรุปได้โดยเข้าใจผลลัพธ์ต่างกัน
- ผู้ใช้เข้าใจว่า `53/53` คือโครงสร้าง ไม่ใช่จำนวนช่องที่ต้องมีข้อมูล
- ทุกคอลัมน์ต้นฉบับที่มีข้อมูลมีปลายทาง, เหตุผลไม่นำเข้า หรือสถานะที่ยังต้องแก้
- ผู้ใช้แยกได้ว่าระบบแนะนำหรือผู้ใช้เลือกเอง
- UI ไม่อนุญาตปลายทางซ้ำโดยไม่ตั้งใจ
- ทุกแถวส่งออกที่มีรหัสและชื่อมีประเภทสินทรัพย์
- หัวกลุ่มถูกส่งต่อโดยไม่กลายเป็นแถวสินทรัพย์
- รหัสซ้ำแสดง Warning และไม่ทำให้ข้อมูลถูกลบ
- ชุดชีตที่เลือกไม่ถูกส่งออกบางส่วนแบบเงียบ ๆ
- Draft ไม่หายเมื่อสลับชีตหรือ Revalidation ล้มเหลว
- ไม่มีการแก้เซลล์หรือตัดแถวจาก Advanced Mapping
- ผ่าน keyboard navigation, screen-reader smoke test และ browser QA ด้วยข้อมูลจริง

## Evidence gates ที่ต้องตรวจระหว่าง Implementation

- ตรวจคำที่ยอมรับใน `ประเภทสินทรัพย์` และ `ชนิดสินทรัพย์` จาก Reference ของ Template จริงก่อนแก้ Parser
- ตรวจพฤติกรรมทุก Source Profile ด้วยไฟล์เทศบาลจริง ห้ามสรุปจาก Template เปล่าเพียงไฟล์เดียว
- ตรวจชื่อและลำดับ 53 ช่องจาก Template runtime ไม่ใช้เลขหรือรายการที่คัดลอกไว้ในเอกสาร
- ห้ามถือว่า Typecheck ผ่านเท่ากับ UX เสร็จ ต้องทดสอบ Upload → Review → Validate → Download ใน Browser
